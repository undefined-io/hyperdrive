#!/var/hyperdrive/scripts/overseer
# vim: set ft=sh :
### Usage:
###   assignment-stop <order>
### Options:
###   --help
###
### ship stops current assignment and goes idle

stop hyperdrive-assignment name="${order}" 2>&1 | timestamped "${SCRIPT_NAME}"
