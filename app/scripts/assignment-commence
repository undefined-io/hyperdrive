#!/var/hyperdrive/scripts/overseer
# vim: set ft=sh :
### Usage:
###   assignment-commence <order>
### Options:
###   --help
###
### ship commences with operation based on specified order

start hyperdrive-assignment name="${order}" 2>&1 | timestamped "${SCRIPT_NAME}"
