#!/usr/bin/env bash
### Usage:
###   test
### Options:
###   --help
###
### hyperdrive test script for playing with bash stuff

#--HEAD--
SCRIPT="$(readlink -e -- "${0}")"
SCRIPT_NAME="$(basename "${SCRIPT}")"
SCRIPT_DIR="$(dirname "${SCRIPT}")"
source "${SCRIPT_DIR}/main.source"
#--/HEAD--

apply_order_exports /data/hyperdrive/headquarters/codevariety/orders
env | sort | grep -i publish
