#!/usr/bin/env bash
### Usage:
###   test
### Options:
###   --help
###
### hyperdrive test script for playing with bash stuff

#--HEAD--
SCRIPT="$(readlink -e -- "${0}")"
SCRIPT_NAME="$(basename "${SCRIPT}")"
SCRIPT_DIR="$(dirname "${SCRIPT}")"
source "${SCRIPT_DIR}/main.source"
#--/HEAD--

export REMOTE_HEAD_SHA=ff71e95489d111f3d30f296e9b4fd8e4f378a955
TEST="$(git ls-remote "https://github.com/undefined-io/hyperdrive")"
gitLsRemoteBranchBySha.gawk <<< "${TEST}"
